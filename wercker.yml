# docker box definition
box: java

# defining the dev pipeline
#
#build:
#  steps:
#    -script:
#      name: Build the code
#      code: |
#        ./gradlew --full-stacktrace -q --project-cache-dir=$WERCKER_CACHE_DIR build -x test

test:
  steps:
    -script:
      name: gradletest
      code: |
        ./gradlew --full-stacktrace -q --project-cache-dir=$WERCKER_CACHE_DIR build -x

dev:
  steps:
    - script:
      name: gradle bootRun
      code: |
        ./gradlew bootRun